# This is a Makefile that compiles a simple C program.
# It shows the basic flow: main.c -> a.out -> run it

all: main.c
	@echo "Step 1: Compile main.c into a.out using GCC"
	gcc main.c                # gcc main.c creates a default output file named a.out

	@echo "Step 2: Run the resulting executable (a.out)"
	./a.out                   # Execute the compiled output